{
  "name": "dukelife-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@dukelife/web",
    "dev:mobile": "npm run start --workspace=@dukelife/mobile",
    "build": "npm run build --workspaces"
  }
}
